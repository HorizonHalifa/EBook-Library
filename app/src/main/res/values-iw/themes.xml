<resources>
    <!-- Base application theme for Hebrew (RTL) -->
    <style name="Base.Theme.EBookLibrary" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Apply Hebrew font globally -->
        <item name="android:fontFamily">@font/hebrew_font</item>

        <!-- Text direction aligns with system language -->
        <item name="android:textDirection">locale</item>
    </style>

    <!-- Entry point theme -->
    <style name="Theme.EBookLibrary" parent="Base.Theme.EBookLibrary" />
</resources>
